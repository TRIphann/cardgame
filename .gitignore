## .NET build output
bin/
obj/
*.user
*.suo
.vs/

## Frontend build output (Vite)
frontend/dist/
node_modules/

## Secrets — never commit
backend/src/Arcana.Api/Secrets/
*.firebase-adminsdk*.json
# Ignore JSON files generically, then allow the safe ones below.
*.json
!backend/src/Arcana.Api/appsettings*.json
!create_preset.json
!frontend/src/config/firebase.js
!frontend/package-lock.json
!frontend/package.json
!frontend/tsconfig*.json

## OS
.DS_Store
Thumbs.db

## IDE
.idea/
.vscode/

## Logs
*.log
backend/.api.err
backend/.api.out
.fake

## Upload results
card-upload-results.json

## Card assets (designed via local SVG, part of repo)
# (no longer ignored — card SVGs are committed so Netlify can serve them)
## Vite preview server junk
.vite-preview*.err
.vite-preview*.out
